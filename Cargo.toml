[package]
name = "json_ink"
version = "0.1.0"
edition = "2024"

[dependencies]
indexmap = "2.14.2"
logging = { path = "./logging" }

[features]
logging = ["logging/logging"]
trace = ["logging", "logging/trace"]
